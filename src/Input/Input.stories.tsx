import * as React from 'react';
import Input from './Input';

export default {
  title: 'John/Input',
  component: Input,
};

export const DefaultInput = () => <Input />;
DefaultInput.story = {
  name: 'Input',
};
